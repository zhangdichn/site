`delete` operation is used to delete a storage under current service.

For example, in object storage services, `delete` means delete a bucket.

## Behavior

- Storage service COULD return errors while the storage is not empty.
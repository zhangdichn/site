`create` operation is used to create a storage under current service.

For example, in object storage services, `create` means create a bucket.

## Behavior

- Storage service COULD return errors while the storage exists.